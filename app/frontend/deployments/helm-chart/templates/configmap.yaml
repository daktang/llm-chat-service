apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "llm-chat-service.fullname" . }}-config
  labels:
    {{- include "llm-chat-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}