# Project Summary
The project is an LLM (Large Language Model) Chat Service that enables users to interact with various AI models through the LiteLLM API. It features a modern user interface for chatting with AI, allowing users to select models and send messages seamlessly. The service aims to provide a lightweight frontend application, utilizing Next.js and Vite for optimal performance and user experience.

# Project Module Description
- **Chat Context**: Manages chat state, including selected models and messages.
- **Model Selector**: UI component for selecting the AI model.
- **Chat Input**: Input area for user messages.
- **Chat Window**: Displays chat messages and conversation history.
- **API Layer**: Handles communication with the LiteLLM API for fetching models and sending chat messages.
- **Deployment**: Includes Docker and Helm configurations for easy deployment.

# Directory Tree
```
/workspace/app/frontend/
├── .envrc                              # Environment variables
├── .env.local                          # Vite-specific environment variables
├── Dockerfile                          # Multi-stage Docker build
├── deployments/
│   └── helm-chart/
│       ├── Chart.yaml
│       ├── values.yaml
│       └── templates/
│           ├── deployment.yaml
│           ├── service.yaml
│           ├── configmap.yaml
│           └── ingress.yaml
├── src/
│   ├── context/
│   │   └── ChatContext.tsx              # Chat state management
│   ├── components/
│   │   ├── ModelSelector.tsx            # Model selection UI
│   │   ├── ChatMessage.tsx               # Message bubble component
│   │   ├── ChatInput.tsx                 # Input area component
│   │   └── ChatWindow.tsx                # Main chat window
│   ├── lib/
│   │   └── api.ts                        # LiteLLM API calls
│   └── pages/
│       └── Index.tsx                     # Main page
└── package.json                          # Project dependencies
```

# File Description Inventory
- **.envrc**: Contains environment variable configurations for LiteLLM.
- **Dockerfile**: Defines the build process for the application using multi-stage builds.
- **helm-chart/**: Contains Helm chart files for deployment.
- **src/**: Contains all source code, including components, context, and pages.

# Technology Stack
- **Frontend**: Next.js, React, Vite
- **Styling**: Tailwind CSS
- **API Communication**: Fetch API
- **Deployment**: Docker, Helm

# Usage
1. **Environment Setup**: Create a `.envrc` and `.env.local` file with the following content:
   ```
   VITE_LITELLM_BASE_URL=https://openllm.net
   VITE_LITELLM_API_KEY=your-api-key-here
   ```
2. **Install Dependencies**: Run `pnpm install` in the project directory.
3. **Build the Application**: Use `pnpm run build` to compile the application.
4. **Run the Application**: Use Docker or Helm for deployment.
